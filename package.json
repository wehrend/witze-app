{
  "name": "witze-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sass": "sass --watch styles:output"
  },
  "devDependencies": {
    "vite": "^8.0.9"
  },
  "dependencies": {
    "sass": "^1.99.0"
  }
}
